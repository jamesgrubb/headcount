---
title: Head Count
---
{% set gap = 1 %}
{% set title = "text-5xl uppercase xyz-in lg:text-9xl mb-6 md:text-8xl font-tall" %}
{% extends 'base.njk' %}
{% block content %}
    {% from "button.njk" import button %}
    <div data-barba="container" x-data="app()" x-init="init($refs)">
        <div data-barba="wrapper" :class="{'bg-green-50': !scrollTop}" @scroll.window="scrollTop = (window.pageYOffset > 50) ? false : true" class="fixed inset-x-0 top-0 z-10">
            <header class="container py-4">
                <nav>
                    <h1 class="grid grid-flow-col gap-1 text-xl font-tall auto-cols-max">
                        <a class="grid grid-flow-col gap-1 auto-cols-max" href="/">
                            <span>head</span><span>count</span></a>
                        <a href="/heads">hands</a>
                    </h1>
                </nav>
            </header>
        </div>
        <section class="relative w-full h-screen overflow-hidden bg-green-200">
            <div class="container flex flex-col items-start justify-start h-full px-4 py-4 lg:py-32 text-brand-500 lg:flex-row">
                <div class="z-10 flex flex-col items-center justify-around w-full h-full py-4 space-y-4 text-center transform translate-x-0 md:justify-center sm:h-full sm:w-full xl:h-2/3 lg:w-2/3">
                    <div class="">
                        <h2 xyz="fade in up duration-20" class="{{ title }}">head count</h2>
                        <p xyz="fade in down duration-20 delay-2" class="px-8 text-center sm:max-w-prose sm:text-xl xyz-in">
                            Get a quick and dirty headcount from any picture on your desktop or device
                        </p>
                    </div>
                    {{ button("start counting")}}
                </div>
                <div class="z-10 self-end w-full transform translate-x-0 sm:self-center sm:w-1/3 h-2/3 sm:h-full">
                    {% include "./assets/phone.svg" %}
                </div>

            </div>
            {% include './assets/group.njk' %}
        </section>
        <section class="w-full h-screen what bg-brand-500 text-brand-300">
            <div class="container relative flex-wrap block h-full py-16 text-center sm:flex">
                <div class="flex flex-col flex-wrap items-center justify-center h-full sm:w-1/3">
                    <h2 class="text-brand-300 {{title}}">what</h2>
                    <p>What is this? You may well ask. I'll let the chaps explain</p>
                </div>
                <div class="grid h-full grid-cols-3 grid-rows-3 gap-3 ml-auto sm:w-3/5">
                    <div x-ref="bubble" class="relative self-center hidden w-full p-12 text-2xl bg-green-300 rounded bubble text-brand-500" :class="speech.grid">
                        <p class="" x-text="speech.text"></p>
                    </div>
                    <img class="w-1/2 col-start-1 row-start-1 place-self-center col-span-full row-span-full" src="https://res.cloudinary.com/makingthings/image/upload/e_grayscale,e_tint:100:4F717A:0p:BBDAE2:100p/v1607425924/headcounthands/Group.png" alt="group">
                    <button class="col-start-1 row-start-1 place-self-center col-span-full row-span-full" @click="play()">
                        <svg xmln="http://www.w3.org/2000/svg" viewbox="0 0 10 10" class="h-24 text-hot">
                            <circle cx="50%" cy="50%" r="4" fill="none" stroke="currentColor" stroke-width="0.5"/>
                        </svg>
                    </div>
                    <div x-cloack="x-cloack" class="absolute top-0 flex flex-wrap w-2/3 sm:w-1/2">
                        <template x-for="i in count">
                            <div class="w-4 h-4 m-4 sm:w-8 sm:h-8 person-icon">
                                {% include "./assets/person-icon.svg" %}
                            </div>
                        </template>
                    </div>

                </div>
            </section>
        </div>
    {% endblock %}

    {% block scripts %}

        <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.5.1/gsap.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.5.1/ScrollTrigger.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/svg.js/3.0.16/svg.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/@barba/core"></script>
        <script src="js/script.js" defer="defer"></script>
    {% endblock %}